import { COLORS } from '../../constants/variables';

export default {
  buttonProps: {
    justifyContent: 'center',
    alignContent: 'center',
    width: 40,
    height: 40,
  },
  iconProps: {
    iconName: 'bars',
    iconSize: 24,
    iconColor: COLORS.white,
  },
};
